function createObj(value) {
    // 이 함수를 구현하라
    return { value: value};
  }
  
  console.log(createObj(3));
  console.log(createObj("hello"));
  
// 파라미터로 전달된 value 값이 value 속성으로 들어있는 객체를 생성하여 리턴하도록

// 출력
// { value: 3 }
// { value: 'hello' }